
{% extends 'base.html.twig' %}

{% block body %}
        
    {{form_start(form)}}

        {{form_row(form.username,{'label':'Username / Nom Utilisateur'})}}
        {{form_row(form.eMail,{'label':'Email / Adresse mail'})}}      
        {{form_row(form.pwd1,{'label':'Password / Mot de passe'})}}     
        {{form_row(form.pwd2 ,{'label':'Password Confirmation / confirmer le mot de passe '})}}

            <br>
                <br>     
                    <button type="submit" class="btn btn-primary"> Submit / Soumettre </button> 
    {{form_end(form )}}
{% endblock %}
